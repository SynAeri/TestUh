# AI-Powered Incident Response Platform - Backend Dependencies
# Connects Claude coding context to production incidents for faster resolution

# Core web framework
fastapi==0.115.0
uvicorn==0.32.1
python-dotenv==1.0.1
pydantic==2.10.3

# HTTP clients
requests==2.32.5

# AI/LLM integration
anthropic==0.39.0  # Optional: Anthropic Claude
google-generativeai==0.8.3  # Google Gemini (primary)

# Database - Supabase client for AI session logging
supabase==2.3.4
httpx<0.26,>=0.24

# Optional: Database (lightweight for hackathon MVP)
# For production, use proper DB. For demo, can use in-memory or JSON files.
